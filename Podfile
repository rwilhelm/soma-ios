platform :ios, '10.0'
use_frameworks!

target 'SoMA' do
    pod 'SQLite.swift', '~> 0.11.3'
    pod 'Firebase/Core'
    pod 'Gloss', '~> 1.2'
    pod 'Alamofire', '~> 4.4'
    pod 'SwiftyJSON'

    target 'SoMATests' do
        inherit! :search_paths
        pod 'Firebase'
    end
end
